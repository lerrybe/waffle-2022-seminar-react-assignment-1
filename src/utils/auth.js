export const checkValidId = (id) => {
  // TODO: validity 검증
  // 현재는 입력만 되면 로그인 되게 구현
  return !id ? false : true;
};

export const checkValidPassword = (password) => {
  // TODO: validity 검증
  // 현재는 입력만 되면 로그인 되게 구현
  return !password ? false : true;
};
